export const ExperienceContent = () => import('../../../../components/ExperienceContent.vue' /* webpackChunkName: "components/experience-content" */).then(c => wrapFunctional(c.default || c))
export const NavMenu = () => import('../../../../components/NavMenu.vue' /* webpackChunkName: "components/nav-menu" */).then(c => wrapFunctional(c.default || c))
export const PassionProjectsContent = () => import('../../../../components/PassionProjectsContent.vue' /* webpackChunkName: "components/passion-projects-content" */).then(c => wrapFunctional(c.default || c))
export const Squares = () => import('../../../../components/Squares.vue' /* webpackChunkName: "components/squares" */).then(c => wrapFunctional(c.default || c))
export const TopBar = () => import('../../../../components/TopBar.vue' /* webpackChunkName: "components/top-bar" */).then(c => wrapFunctional(c.default || c))
export const Tutorial = () => import('../../../../components/Tutorial.vue' /* webpackChunkName: "components/tutorial" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
